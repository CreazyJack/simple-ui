import { FC } from 'react';
interface RotateTransitionProps {
    active: boolean;
    timeout?: number;
    wrapper?: boolean;
}
declare const RotateTransition: FC<RotateTransitionProps>;
export default RotateTransition;
